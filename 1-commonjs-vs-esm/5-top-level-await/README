One of the most significant differences between ESM and CJS is that ESM allows top-level await.

In CommonJS, imports are synchronous.
You can't do anything asynchronous at the top level, because it would make the import asynchrounous.

In ESM, you're likely to need to wait on the network any time you import code, so all imports are asynchronous.
